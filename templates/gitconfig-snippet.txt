# git 設定スニペット — 日本語ファイル名をそのまま表示する
#
# 適用方法(どちらか):
#   (A) コマンドで1回:  git config --global core.quotepath false
#   (B) ~/.gitconfig に下記を追記
#
# 効果: git status 等で「\350\251\225...」ではなく「評価...」と日本語表示される。
# リスク: ほぼ無し(表示のみの変更。ファイル操作の実体には影響しない)。

[core]
    quotepath = false
